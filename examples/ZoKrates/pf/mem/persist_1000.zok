// persistent RAM

const u32 LEN = 5000
const field ACC = 500

def main(committed field[LEN] array, private field x) -> field:
    field acc = 0
    for field i in 0..ACC do
        acc = acc + array[x + i]
    endfor
    return acc
